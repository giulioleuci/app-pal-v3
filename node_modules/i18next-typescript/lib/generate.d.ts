export declare type Options = {
    inFolder: string;
    outFile: string;
    defaultNs?: string;
    indent?: number;
    typeName?: string;
    quoteChar?: string;
};
export declare const generate: ({ inFolder, outFile, defaultNs, indent, typeName, quoteChar, }: Options) => Promise<void>;
